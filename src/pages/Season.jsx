const fields=['champion','pichichi','zarra','leastConceded','topScorerTeam','firstCoachSacked'];
export default function Season({ season, setSeason, saveSeason, isAdmin }) { return <section className="page"><h1>Tablas previas</h1><p>Fecha límite por defecto: 14 de agosto, 12:00.</p>{fields.map(f=><label className="field" key={f}>{f}<input value={season[f]||''} onChange={e=>setSeason({...season,[f]:e.target.value})}/></label>)}<label className="field">Doble<select value={season.doubleKey||''} onChange={e=>setSeason({...season,doubleKey:e.target.value})}><option/> {fields.map(f=><option key={f}>{f}</option>)}</select></label>{['champions','europa','relegation','promotion','rfefDrop'].map(f=><label className="field" key={f}>{f}<textarea value={(season[f]||[]).join('\n')} onChange={e=>setSeason({...season,[f]:e.target.value.split('\n').filter(Boolean)})}/></label>)}<button className="primary" onClick={saveSeason}>Guardar tablas</button>{isAdmin&&<p className="notice">Como admin puedes guardar resultados finales en seasonResults y recalcular puntos.</p>}</section> }
