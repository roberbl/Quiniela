export default function Ranking({ users, teams }) { const ranked=[...users].sort((a,b)=>(b.totalPoints||0)-(a.totalPoints||0)); return <section className="page"><h1>Clasificación</h1>{ranked.map((u,i)=><article className="card rank" key={u.id}><b>#{i+1} {u.name}</b><span>{u.totalPoints||0} pts</span><small>Dinero: {u.money||0}€ · Copitas: {u.copitaCount||0} · Tablas: {u.seasonPoints||0} · Equipo: {teams.find(t=>t.id===u.teamId)?.name}</small></article>)}<h2>Equipos</h2>{teams.map(t=><article className="card" key={t.id}><b>{t.name}</b><span>{users.filter(u=>u.teamId===t.id).reduce((s,u)=>s+(u.totalPoints||0),0)} pts</span></article>)}</section>; }
